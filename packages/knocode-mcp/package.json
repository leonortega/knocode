{
  "name": "knocode-mcp",
  "version": "0.9.11",
  "description": "Knocode MCP server — stdio → HTTP pass-through proxy to the daemon-hosted MCP (POST /mcp). Exposes the daemon's canonical tool (knocode_context) to any stdio MCP agent: Codex, VS Code Copilot, Claude.",
  "type": "module",
  "bin": {
    "knocode-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/node": "^22.7.0"
  }
}
